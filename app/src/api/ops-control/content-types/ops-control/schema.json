{
  "kind": "singleType",
  "collectionName": "ops_controls",
  "info": {
    "singularName": "ops-control",
    "pluralName": "ops-controls",
    "displayName": "Ops Control",
    "description": "Owner-only runtime gate for Ops pages."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "opsEnabledUntil": {
      "type": "datetime",
      "required": false,
      "description": "Ops pages are enabled while now < opsEnabledUntil."
    },
    "opsViewTokenHash": {
      "type": "string",
      "required": true,
      "minLength": 64,
      "maxLength": 64,
      "description": "SHA-256 hash (hex) of the owner ops view token."
    },
    "opsNote": {
      "type": "text",
      "required": false
    }
  }
}
