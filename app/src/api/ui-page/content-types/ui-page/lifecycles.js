'use strict';

const { createLanguageStateLifecycle } = require('../../../../modules/language-state');

module.exports = createLanguageStateLifecycle({
  uid: 'api::ui-page.ui-page',
  contextLabel: 'ui-page',
});
