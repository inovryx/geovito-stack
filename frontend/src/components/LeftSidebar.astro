---
import LanguageSwitcher from './LanguageSwitcher.astro';
import { translate } from '../lib/uiLanguage';
import { resolveStrapiBaseUrl } from '../lib/strapiConfig';

interface Props {
  currentLanguage: string;
  languageLinks: Record<string, string>;
  ui: Record<string, unknown>;
  hideSiteNavigation?: boolean;
}

const { currentLanguage, languageLinks, ui, hideSiteNavigation = false } = Astro.props as Props;
const isTruthy = (value: string | undefined) => ['1', 'true', 'yes', 'on'].includes(String(value || '').trim().toLowerCase());
const siteLockdownEnabled = isTruthy(
  (import.meta.env.PUBLIC_SITE_LOCKDOWN_ENABLED as string | undefined) || process.env.PUBLIC_SITE_LOCKDOWN_ENABLED
);
const loginPath = `/${currentLanguage}/login/`;
const registerPath = `/${currentLanguage}/register/`;
const dashboardPath = `/${currentLanguage}/dashboard/`;
const accountPath = `/${currentLanguage}/account/`;
const accountCommentsPath = `${accountPath}?commentState=pending#comments`;
const accountSavedListsPath = `${accountPath}#saved-lists`;
const accountLocalePath = `${accountPath}#locale-progress`;
const dashboardEditorialPath = `${dashboardPath}#dashboard-editorial-moderation`;
const dashboardReportsPath = `${dashboardPath}#dashboard-editorial-reports`;
const dashboardAccountRequestsPath = `${dashboardPath}#dashboard-editorial-account-requests`;
const dashboardLocalePath = `${dashboardPath}#dashboard-editorial-locale`;
const dashboardControlPath = `${dashboardPath}#dashboard-control`;
const dashboardAdsPath = `${dashboardPath}#dashboard-control-ads`;
const authBaseUrl = resolveStrapiBaseUrl({
  STRAPI_URL: import.meta.env.STRAPI_URL as string | undefined,
  PUBLIC_STRAPI_URL: import.meta.env.PUBLIC_STRAPI_URL as string | undefined,
  CF_PAGES: process.env.CF_PAGES,
  NODE_ENV: process.env.NODE_ENV,
  ALLOW_LOCALHOST_STRAPI:
    process.env.ALLOW_LOCALHOST_STRAPI || (import.meta.env.ALLOW_LOCALHOST_STRAPI as string | undefined),
});
const strapiAdminUrl = `${authBaseUrl.replace(/\/$/, '')}/admin`;
const ownerEmailHintsCsv = String(
  (import.meta.env.PUBLIC_OWNER_EMAILS as string | undefined) ||
    (import.meta.env.PUBLIC_OWNER_EMAIL as string | undefined) ||
    ''
)
  .split(',')
  .map((entry) => String(entry || '').trim().toLowerCase())
  .filter(Boolean)
  .join(',');
const ownerEmailHint = ownerEmailHintsCsv.split(',')[0] || '';
const loginLabel = translate(ui, 'auth.login', {}, 'Login');
const registerLabel = translate(ui, 'auth.register', {}, 'Register');
const dashboardLabel = translate(ui, 'dashboard.title', {}, 'Dashboard');
const accountLabel = translate(ui, 'nav.account', {}, 'Account');
const logoutLabel = translate(ui, 'auth.logout', {}, 'Logout');
const signedInLabel = translate(ui, 'auth.signedInAs', {}, 'Signed in');
const lockdownHint = translate(ui, 'auth.lockdownHint', {}, 'Test mode active');
const adminToolsLabel = translate(ui, 'dashboard.controlTitle', {}, 'Control center');
const adminRoleLabel = translate(ui, 'dashboard.accessRole', {}, 'Role');
const accessMemberLabel = translate(ui, 'dashboard.accessMember', {}, 'Member');
const accessEditorLabel = translate(ui, 'dashboard.accessEditor', {}, 'Editor');
const accessAdminLabel = translate(ui, 'dashboard.accessAdmin', {}, 'Admin');
const accessOwnerLabel = translate(ui, 'dashboard.accessOwner', {}, 'Owner');
const adminModerationLabel = translate(ui, 'dashboard.moderationQueueTitle', {}, 'Pending moderation queue');
const adminReportsLabel = translate(ui, 'dashboard.reportInboxTitle', {}, 'Report inbox');
const adminAccountRequestsLabel = translate(ui, 'dashboard.accountRequestsTitle', {}, 'Account requests');
const adminLocaleLabel = translate(ui, 'dashboard.localeTitle', {}, 'Translation visibility');
const adminSeoLabel = translate(ui, 'dashboard.moduleSeo', {}, 'SEO');
const adminAdsLabel = translate(ui, 'dashboard.moduleAds', {}, 'Ads');
const adminControlLabel = translate(ui, 'dashboard.controlAdmin', {}, 'Open Strapi admin');
const workspaceTitle = translate(ui, 'dashboard.workspaceTitle', {}, 'Workspace');
const workspaceHint = translate(ui, 'dashboard.workspaceHint', {}, 'Personal and role-based shortcuts.');
const workspaceCommentsLabel = translate(ui, 'dashboard.quickComments', {}, 'Open my comment queue');
const workspaceSavedListsLabel = translate(ui, 'dashboard.savedListsManage', {}, 'Open saved lists');
const workspaceLocaleLabel = translate(ui, 'dashboard.quickLocale', {}, 'Open translation progress');
const dashboardModuleTitle = translate(ui, 'dashboard.moduleNavTitle', {}, 'Dashboard modules');
const dashboardModuleGeneral = translate(ui, 'dashboard.moduleGeneral', {}, 'General');
const dashboardModuleSeo = translate(ui, 'dashboard.moduleSeo', {}, 'SEO');
const dashboardModuleAds = translate(ui, 'dashboard.moduleAds', {}, 'Ads');
const dashboardModuleModeration = translate(ui, 'dashboard.moduleModeration', {}, 'Moderation');
const dashboardModuleTranslation = translate(ui, 'dashboard.moduleTranslation', {}, 'Translation');
const dashboardModuleSettings = translate(ui, 'dashboard.moduleSettings', {}, 'Settings');
const dashboardSessionTitle = translate(ui, 'dashboard.overviewTitle', {}, 'Session overview');
const dashboardSessionIdentityLabel = translate(ui, 'dashboard.overviewIdentity', {}, 'Identity');
const isDashboardRoute = (() => {
  const parts = String(Astro.url.pathname || '')
    .split('/')
    .filter(Boolean);
  return parts.length >= 2 && parts[1] === 'dashboard';
})();
const hideSiteNavigationResolved = hideSiteNavigation || isDashboardRoute;
---

<div
  class={`left-sidebar-stack ${isDashboardRoute ? 'is-dashboard-route' : ''} ${
    hideSiteNavigationResolved ? 'hide-site-navigation' : ''
  }`}
>
  <section
    class="sidebar-block sidebar-dashboard-modules gv-card surface-card card-compact bg-surface border-border"
    data-auth-dashboard-shell
    hidden
  >
    <h2 class="sidebar-title">{dashboardModuleTitle}</h2>
    <ul class="sidebar-admin-list sidebar-dashboard-modules-list">
      <li>
        <a class="small-link" href={`${dashboardPath}#dashboard-member`} data-auth-dashboard-link data-min-role="member">
          {dashboardModuleGeneral}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardControlPath} data-auth-dashboard-link data-min-role="admin" hidden>
          {dashboardModuleSeo}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardAdsPath} data-auth-dashboard-link data-min-role="admin" hidden>
          {dashboardModuleAds}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardEditorialPath} data-auth-dashboard-link data-min-role="editor" hidden>
          {dashboardModuleModeration}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardLocalePath} data-auth-dashboard-link data-min-role="editor" hidden>
          {dashboardModuleTranslation}
        </a>
      </li>
      <li>
        <a class="small-link" href={`${dashboardPath}#dashboard-member-settings`} data-auth-dashboard-link data-min-role="member">
          {dashboardModuleSettings}
        </a>
      </li>
    </ul>
  </section>

  <section
    class="sidebar-block sidebar-dashboard-session gv-card surface-card card-compact bg-surface border-border"
    data-auth-dashboard-session
    data-identity-label={dashboardSessionIdentityLabel}
    data-role-prefix={adminRoleLabel}
    data-role-member={accessMemberLabel}
    data-role-editor={accessEditorLabel}
    data-role-admin={accessAdminLabel}
    data-role-owner={accessOwnerLabel}
    hidden
  >
    <h2 class="sidebar-title">{dashboardSessionTitle}</h2>
    <p class="small sidebar-auth-user" data-auth-dashboard-identity>
      {signedInLabel}: -
    </p>
    <p class="small sidebar-auth-user" data-auth-dashboard-role>
      {adminRoleLabel}: {accessMemberLabel}
    </p>
    <div class="sidebar-auth-row">
      <a href={dashboardPath} class="small-link">{dashboardLabel}</a>
      <a href={accountPath} class="small-link">{accountLabel}</a>
      <button type="button" class="small-link sidebar-auth-logout" data-auth-logout>{logoutLabel}</button>
    </div>
  </section>

  <section
    class="sidebar-block sidebar-auth-links gv-card surface-card card-compact bg-surface border-border"
    data-auth-shell
    data-signed-in-label={signedInLabel}
    data-auth-base-url={authBaseUrl}
    data-owner-email-hints={ownerEmailHintsCsv}
    data-owner-email-hint={ownerEmailHint}
  >
    <p class="small sidebar-auth-user" data-auth-user hidden></p>

    <div class="sidebar-auth-row" data-auth-logged-out>
      <a href={loginPath} class="small-link">{loginLabel}</a>
      {
        siteLockdownEnabled ? (
          <span class="small-link sidebar-auth-disabled" aria-disabled="true">
            {lockdownHint}
          </span>
        ) : (
          <a href={registerPath} class="small-link">{registerLabel}</a>
        )
      }
    </div>

    <div class="sidebar-auth-row" data-auth-logged-in hidden>
      <a href={dashboardPath} class="small-link">{dashboardLabel}</a>
      <a href={accountPath} class="small-link">{accountLabel}</a>
      <button type="button" class="small-link sidebar-auth-logout" data-auth-logout>{logoutLabel}</button>
    </div>
  </section>

  <section
    class="sidebar-block sidebar-workspace gv-card surface-card card-compact bg-surface border-border"
    data-auth-workspace-shell
    hidden
  >
    <h2 class="sidebar-title">{workspaceTitle}</h2>
    <p class="small sidebar-workspace-hint">{workspaceHint}</p>
    <ul class="sidebar-admin-list sidebar-workspace-list">
      <li>
        <a class="small-link" href={dashboardPath} data-auth-workspace-link data-min-role="member">
          {dashboardLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={accountPath} data-auth-workspace-link data-min-role="member">
          {accountLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={accountCommentsPath} data-auth-workspace-link data-min-role="member">
          {workspaceCommentsLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={accountSavedListsPath} data-auth-workspace-link data-min-role="member">
          {workspaceSavedListsLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={accountLocalePath} data-auth-workspace-link data-min-role="editor" hidden>
          {workspaceLocaleLabel}
        </a>
      </li>
    </ul>
  </section>

  <section
    class="sidebar-block sidebar-admin-tools gv-card surface-card card-compact bg-surface border-border"
    data-auth-admin-shell
    data-role-prefix={adminRoleLabel}
    data-role-member={accessMemberLabel}
    data-role-editor={accessEditorLabel}
    data-role-admin={accessAdminLabel}
    data-role-owner={accessOwnerLabel}
    hidden
  >
    <h2 class="sidebar-title">{adminToolsLabel}</h2>
    <p class="small sidebar-auth-user" data-auth-admin-role>
      {adminRoleLabel}: {accessMemberLabel}
    </p>
    <ul class="sidebar-admin-list">
      <li>
        <a class="small-link" href={dashboardEditorialPath} data-auth-admin-link data-min-role="editor" hidden>
          {adminModerationLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardReportsPath} data-auth-admin-link data-min-role="editor" hidden>
          {adminReportsLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardAccountRequestsPath} data-auth-admin-link data-min-role="editor" hidden>
          {adminAccountRequestsLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardLocalePath} data-auth-admin-link data-min-role="editor" hidden>
          {adminLocaleLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardControlPath} data-auth-admin-link data-min-role="admin" hidden>
          {adminSeoLabel}
        </a>
      </li>
      <li>
        <a class="small-link" href={dashboardAdsPath} data-auth-admin-link data-min-role="admin" hidden>
          {adminAdsLabel}
        </a>
      </li>
      <li>
        <a
          class="small-link"
          href={strapiAdminUrl}
          target="_blank"
          rel="noopener noreferrer"
          data-auth-admin-link
          data-min-role="admin"
          hidden
        >
          {adminControlLabel}
        </a>
      </li>
    </ul>
  </section>

  <section
    class="sidebar-block sidebar-language-switch sidebar-language-switch-dock gv-card surface-card card-compact bg-surface border-border"
  >
    <h2 class="sidebar-title">{translate(ui, 'layout.languageTitle', {}, 'Language')}</h2>
    <LanguageSwitcher currentLanguage={currentLanguage} languageLinks={languageLinks} ui={ui} />
  </section>
</div>

<script>
  (() => {
    if (window.__geovitoAuthSidebarBound) return;
    window.__geovitoAuthSidebarBound = true;

    const isDashboardRoute = (() => {
      const parts = String(window.location.pathname || '')
        .split('/')
        .filter(Boolean);
      return parts.length >= 2 && parts[1] === 'dashboard';
    })();

    const resolveSessionEmail = (parsed) => {
      const primary = typeof parsed?.email === 'string' ? parsed.email.trim() : '';
      if (primary) return primary;
      const nested = typeof parsed?.user?.email === 'string' ? parsed.user.email.trim() : '';
      if (nested) return nested;
      const legacy = typeof parsed?.identifier === 'string' ? parsed.identifier.trim() : '';
      if (legacy.includes('@')) return legacy;
      return '';
    };

    const readSession = () => {
      try {
        const fromLocal = localStorage.getItem('geovito_auth_session');
        const fromSession = sessionStorage.getItem('geovito_auth_session');
        const raw = fromLocal || fromSession;
        if (!raw) return null;

        if (!fromLocal && fromSession) {
          localStorage.setItem('geovito_auth_session', fromSession);
        }

        const parsed = JSON.parse(raw);
        if (!parsed || typeof parsed !== 'object') return null;
        const jwt = typeof parsed.jwt === 'string' ? parsed.jwt.trim() : '';
        if (!jwt) return null;
        const username =
          typeof parsed.username === 'string'
            ? parsed.username.trim()
            : typeof parsed?.user?.username === 'string'
              ? parsed.user.username.trim()
              : '';
        const email = resolveSessionEmail(parsed);
        return { username, email, jwt };
      } catch {
        return null;
      }
    };

    const normalize = (value) => String(value || '').trim().toLowerCase();
    const roleOrder = {
      member: 0,
      editor: 1,
      admin: 2,
      owner: 3,
    };
    const dashboardHashAliases = {
      '#dashboard-general': '#dashboard-member',
      '#dashboard-settings': '#dashboard-member-settings',
      '#dashboard-moderation': '#dashboard-editorial-moderation',
      '#dashboard-reports': '#dashboard-editorial-reports',
      '#dashboard-account-requests': '#dashboard-editorial-account-requests',
      '#dashboard-translation': '#dashboard-editorial-locale',
      '#dashboard-seo': '#dashboard-control',
      '#dashboard-control-seo': '#dashboard-control',
      '#dashboard-ads': '#dashboard-control-ads',
    };
    const DASHBOARD_NAV_SYNC_EVENT = 'geovito:dashboard-nav-sync';
    const normalizeHash = (value) => {
      const hash = normalize(value);
      return dashboardHashAliases[hash] || hash;
    };

    const isSidebarLinkActive = (href, fallbackWhenNoHash = false) => {
      const value = String(href || '').trim();
      if (!value) return false;
      try {
        const target = new URL(value, window.location.origin);
        const current = new URL(window.location.href);
        if (target.pathname !== current.pathname) return false;
        const targetHash = normalizeHash(target.hash || '');
        const currentHash = normalizeHash(current.hash || '');
        if (!targetHash) return true;
        if (!currentHash && fallbackWhenNoHash) return true;
        if (targetHash !== currentHash) return false;
        return true;
      } catch {
        return false;
      }
    };

    const parseOwnerHints = (value) => {
      const values = String(value || '')
        .split(',')
        .map((entry) => normalize(entry))
        .filter(Boolean);
      return Array.from(new Set(values));
    };

    const inferRole = (payload, session, ownerHints) => {
      const roleRaw = normalize(payload?.role?.type || payload?.role?.name || payload?.role?.code);
      if (roleRaw.includes('super') || roleRaw.includes('admin') || roleRaw.includes('administrator')) {
        return 'admin';
      }
      if (roleRaw.includes('editor')) {
        return 'editor';
      }
      const email = normalize(payload?.email || session?.email || '');
      if (email && ownerHints.includes(email)) {
        return 'owner';
      }
      return 'member';
    };

    const createRequestId = (scope = 'sidebar') => {
      const tag = String(scope || 'sidebar')
        .trim()
        .toLowerCase()
        .replace(/[^a-z0-9_-]+/g, '-')
        .slice(0, 24) || 'sidebar';
      if (typeof crypto !== 'undefined' && typeof crypto.randomUUID === 'function') {
        return `gv-${tag}-${crypto.randomUUID()}`;
      }
      return `gv-${tag}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 10)}`;
    };

    const resolveSessionRole = async (session) => {
      const shells = Array.from(document.querySelectorAll('[data-auth-shell]'));
      const firstShell = shells.find((shell) => shell instanceof HTMLElement);
      const authBaseUrl = firstShell instanceof HTMLElement
        ? String(firstShell.getAttribute('data-auth-base-url') || '').trim()
        : '';
      const ownerHints = firstShell instanceof HTMLElement
        ? (() => {
            const values = parseOwnerHints(firstShell.getAttribute('data-owner-email-hints') || '');
            if (values.length > 0) return values;
            const legacy = normalize(firstShell.getAttribute('data-owner-email-hint') || '');
            return legacy ? [legacy] : [];
          })()
        : [];

      if (!session) {
        return 'member';
      }

      if (!authBaseUrl) {
        return ownerHints.includes(normalize(session.email)) ? 'owner' : 'member';
      }

      if (!session.jwt) {
        return 'member';
      }

      try {
        const response = await fetch(`${authBaseUrl}/api/users/me?populate=role`, {
          headers: {
            Accept: 'application/json',
            Authorization: `Bearer ${session.jwt}`,
            'X-Request-Id': createRequestId('sidebar-role-load'),
          },
        });
        if (!response.ok) {
          return ownerHints.includes(normalize(session.email)) ? 'owner' : 'member';
        }
        const payload = await response.json().catch(() => null);
        if (!payload || typeof payload !== 'object') {
          return ownerHints.includes(normalize(session.email)) ? 'owner' : 'member';
        }
        return inferRole(payload, session, ownerHints);
      } catch {
        return ownerHints.includes(normalize(session.email)) ? 'owner' : 'member';
      }
    };

    const syncSidebarActiveLinks = () => {
      document.querySelectorAll('[data-auth-dashboard-link]').forEach((node) => {
        if (!(node instanceof HTMLElement)) return;
        if (node.hidden) {
          node.classList.remove('is-active');
          return;
        }
        const href = node.getAttribute('href') || '';
        const fallbackWhenNoHash = href.endsWith('#dashboard-member');
        node.classList.toggle('is-active', isSidebarLinkActive(href, fallbackWhenNoHash));
      });

      document.querySelectorAll('[data-auth-workspace-link]').forEach((node) => {
        if (!(node instanceof HTMLElement)) return;
        if (node.hidden) {
          node.classList.remove('is-active');
          return;
        }
        const href = node.getAttribute('href') || '';
        node.classList.toggle('is-active', isSidebarLinkActive(href));
      });

      document.querySelectorAll('[data-auth-admin-link]').forEach((node) => {
        if (!(node instanceof HTMLElement)) return;
        if (node.hidden) {
          node.classList.remove('is-active');
          return;
        }
        const href = node.getAttribute('href') || '';
        node.classList.toggle('is-active', isSidebarLinkActive(href));
      });
    };

    const applyAuthState = async () => {
      const session = readSession();
      const role = session ? await resolveSessionRole(session) : 'member';
      const roleRank = Number(roleOrder[role] ?? 0);
      const identity = session ? (session.username || session.email) : '';
      const previewEligible = roleRank >= roleOrder.admin;

      document.documentElement.setAttribute('data-site-language-preview-eligible', previewEligible ? '1' : '0');
      window.dispatchEvent(
        new CustomEvent('geovito:site-language-preview-role', {
          detail: { eligible: previewEligible, role },
        })
      );

      document.querySelectorAll('.left-sidebar-stack').forEach((stack) => {
        if (!(stack instanceof HTMLElement)) return;
        stack.classList.toggle('is-dashboard-mode', isDashboardRoute);
        stack.classList.toggle('is-dashboard-auth', isDashboardRoute && Boolean(session));
      });

      document.querySelectorAll('[data-auth-shell]').forEach((shell) => {
        if (!(shell instanceof HTMLElement)) return;

        const loggedOut = shell.querySelector('[data-auth-logged-out]');
        const loggedIn = shell.querySelector('[data-auth-logged-in]');
        const userLine = shell.querySelector('[data-auth-user]');
        const signedInPrefix = shell.getAttribute('data-signed-in-label') || 'Signed in';

        if (loggedOut instanceof HTMLElement) {
          loggedOut.hidden = Boolean(session);
        }

        if (loggedIn instanceof HTMLElement) {
          loggedIn.hidden = !session;
        }

        if (userLine instanceof HTMLElement) {
          if (session && identity) {
            userLine.hidden = false;
            userLine.textContent = `${signedInPrefix}: ${identity}`;
          } else {
            userLine.hidden = true;
            userLine.textContent = '';
          }
        }
      });

      document.querySelectorAll('[data-auth-admin-shell]').forEach((shell) => {
        if (!(shell instanceof HTMLElement)) return;

        shell.hidden = !session || roleRank < roleOrder.editor;

        const roleNode = shell.querySelector('[data-auth-admin-role]');
        const rolePrefix = shell.getAttribute('data-role-prefix') || 'Role';
        const shellRoleLabelMap = {
          member: shell.getAttribute('data-role-member') || 'Member',
          editor: shell.getAttribute('data-role-editor') || 'Editor',
          admin: shell.getAttribute('data-role-admin') || 'Admin',
          owner: shell.getAttribute('data-role-owner') || 'Owner',
        };
        if (roleNode instanceof HTMLElement) {
          roleNode.textContent = `${rolePrefix}: ${shellRoleLabelMap[role] || shellRoleLabelMap.member}`;
        }

        shell.querySelectorAll('[data-auth-admin-link]').forEach((node) => {
          if (!(node instanceof HTMLElement)) return;
          const minRole = normalize(node.getAttribute('data-min-role') || 'member');
          const minRank = Number(roleOrder[minRole] ?? roleOrder.member);
          node.hidden = roleRank < minRank;
        });
      });

      document.querySelectorAll('[data-auth-workspace-shell]').forEach((shell) => {
        if (!(shell instanceof HTMLElement)) return;

        shell.hidden = !session;
        if (!session) return;

        shell.querySelectorAll('[data-auth-workspace-link]').forEach((node) => {
          if (!(node instanceof HTMLElement)) return;
          const minRole = normalize(node.getAttribute('data-min-role') || 'member');
          const minRank = Number(roleOrder[minRole] ?? roleOrder.member);
          node.hidden = roleRank < minRank;
        });
      });

      document.querySelectorAll('[data-auth-dashboard-session]').forEach((shell) => {
        if (!(shell instanceof HTMLElement)) return;
        shell.hidden = !session || !isDashboardRoute;
        if (!session || !isDashboardRoute) return;

        const identityNode = shell.querySelector('[data-auth-dashboard-identity]');
        const roleNode = shell.querySelector('[data-auth-dashboard-role]');
        const identityLabel = shell.getAttribute('data-identity-label') || 'Identity';
        const rolePrefix = shell.getAttribute('data-role-prefix') || 'Role';
        const shellRoleLabelMap = {
          member: shell.getAttribute('data-role-member') || 'Member',
          editor: shell.getAttribute('data-role-editor') || 'Editor',
          admin: shell.getAttribute('data-role-admin') || 'Admin',
          owner: shell.getAttribute('data-role-owner') || 'Owner',
        };
        const resolvedRoleLabel = shellRoleLabelMap[role] || shellRoleLabelMap.member;
        if (identityNode instanceof HTMLElement) {
          identityNode.textContent = `${identityLabel}: ${identity || '-'}`;
        }
        if (roleNode instanceof HTMLElement) {
          roleNode.textContent = `${rolePrefix}: ${resolvedRoleLabel}`;
        }
      });

      document.querySelectorAll('[data-auth-dashboard-shell]').forEach((shell) => {
        if (!(shell instanceof HTMLElement)) return;

        shell.hidden = !session || !isDashboardRoute;
        if (!session || !isDashboardRoute) return;

        shell.querySelectorAll('[data-auth-dashboard-link]').forEach((node) => {
          if (!(node instanceof HTMLElement)) return;
          const minRole = normalize(node.getAttribute('data-min-role') || 'member');
          const minRank = Number(roleOrder[minRole] ?? roleOrder.member);
          node.hidden = roleRank < minRank;
        });
      });

      syncSidebarActiveLinks();
    };

    const bindLogout = () => {
      document.querySelectorAll('[data-auth-logout]').forEach((button) => {
        if (!(button instanceof HTMLButtonElement)) return;
        if (button.dataset.authLogoutBound === '1') return;
        button.dataset.authLogoutBound = '1';

        button.addEventListener('click', () => {
          try {
            localStorage.removeItem('geovito_auth_session');
            sessionStorage.removeItem('geovito_auth_session');
          } catch {
            // no-op
          }

          void applyAuthState();

          const language = document.documentElement.lang?.toLowerCase() || 'en';
          window.location.assign(`/${language}/login/`);
        });
      });
    };

    const init = () => {
      bindLogout();
      void applyAuthState();
    };

    window.addEventListener('storage', (event) => {
      if (event.key === 'geovito_auth_session') {
        void applyAuthState();
      }
    });

    window.addEventListener('hashchange', () => {
      syncSidebarActiveLinks();
    });

    window.addEventListener(DASHBOARD_NAV_SYNC_EVENT, () => {
      syncSidebarActiveLinks();
    });

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init, { once: true });
      return;
    }

    init();
  })();
</script>

<style>
.sidebar-auth-disabled {
    color: var(--ink-muted);
    cursor: default;
    text-decoration: none;
}

.sidebar-nav-main-title {
    display: none;
}

.left-sidebar-stack.is-dashboard-route {
    gap: 0.66rem;
}

.left-sidebar-stack.is-dashboard-route .sidebar-block {
    border-radius: 0.8rem;
    box-shadow: 0 10px 22px rgb(15 23 42 / 0.07);
}

.left-sidebar-stack.is-dashboard-route .sidebar-title {
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-size: 0.69rem;
    color: color-mix(in srgb, var(--ink-muted) 92%, #0f172a);
}

.left-sidebar-stack.is-dashboard-route .small {
    line-height: 1.45;
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-modules {
    order: 1;
    border-color: color-mix(in srgb, var(--brand) 30%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 30%, var(--surface-strong));
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-session {
    order: 2;
    border-color: color-mix(in srgb, var(--brand) 26%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 24%, var(--surface-strong));
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-session .sidebar-auth-row {
    margin-top: 0.25rem;
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-modules-list {
    gap: 0.3rem;
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-modules-list .small-link {
    font-size: 0.9rem;
    line-height: 1.25;
    display: flex;
    align-items: center;
    min-height: 1.95rem;
    border: 1px solid transparent;
    border-radius: 0.56rem;
    padding: 0.16rem 0.44rem;
    text-decoration: none;
    font-weight: 600;
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-modules-list .small-link:hover {
    text-decoration: none;
    border-color: color-mix(in srgb, var(--brand) 26%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 24%, var(--surface-subtle));
}

.left-sidebar-stack.is-dashboard-route .sidebar-dashboard-modules-list .small-link.is-active {
    font-weight: 700;
    color: var(--brand-strong);
    text-decoration: none;
    border-color: color-mix(in srgb, var(--brand) 44%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 52%, var(--surface-subtle));
    box-shadow: inset 2px 0 0 color-mix(in srgb, var(--brand) 58%, transparent);
}

.left-sidebar-stack.is-dashboard-route .sidebar-admin-tools .small-link.is-active {
    font-weight: 700;
    color: var(--brand-strong);
    text-decoration: none;
    border-color: color-mix(in srgb, var(--brand) 44%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 52%, var(--surface-subtle));
    box-shadow: inset 2px 0 0 color-mix(in srgb, var(--brand) 58%, transparent);
}

.left-sidebar-stack.is-dashboard-route .sidebar-admin-list .small-link:hover {
    text-decoration: none;
    border-color: color-mix(in srgb, var(--brand) 26%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 24%, var(--surface-subtle));
}

.left-sidebar-stack.is-dashboard-route .sidebar-workspace {
    order: 3;
    border-color: color-mix(in srgb, var(--brand) 26%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 24%, var(--surface-strong));
}

.left-sidebar-stack.is-dashboard-route .sidebar-workspace-list .small-link {
    font-size: 0.9rem;
    line-height: 1.25;
    display: flex;
    align-items: center;
    min-height: 1.95rem;
    border: 1px solid transparent;
    border-radius: 0.56rem;
    padding: 0.16rem 0.44rem;
    text-decoration: none;
    font-weight: 600;
}

.left-sidebar-stack.is-dashboard-route .sidebar-admin-list .small-link {
    font-weight: 600;
}

.left-sidebar-stack.is-dashboard-route .sidebar-workspace-list .small-link:hover {
    text-decoration: none;
    border-color: color-mix(in srgb, var(--brand) 26%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 24%, var(--surface-subtle));
}

.left-sidebar-stack.is-dashboard-route .sidebar-workspace-list .small-link.is-active {
    font-weight: 700;
    color: var(--brand-strong);
    text-decoration: none;
    border-color: color-mix(in srgb, var(--brand) 44%, var(--border));
    background: color-mix(in srgb, var(--brand-soft) 52%, var(--surface-subtle));
    box-shadow: inset 2px 0 0 color-mix(in srgb, var(--brand) 58%, transparent);
}

.left-sidebar-stack.is-dashboard-route .sidebar-auth-links {
    display: none;
}

.left-sidebar-stack.is-dashboard-route .sidebar-admin-tools {
    order: 4;
}

.left-sidebar-stack.is-dashboard-route .sidebar-language-switch {
    display: none;
}

.left-sidebar-stack.hide-site-navigation :global(.app-nav),
.left-sidebar-stack.hide-site-navigation :global(.sidebar-nav-main) {
    display: none !important;
}
</style>
