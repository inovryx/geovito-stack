# Profile alias fallback for Pages static edge:
# - /@username -> /en/@username/ (307)
# Dynamic language-aware redirect may still be handled by app/runtime rules where available.
/@* /en/@:splat 307
